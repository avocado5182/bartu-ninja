let $carousel = $('.main-carousel').flickity({
  // options
  cellAlign: 'center',
  wrapAround: true,
  imagesLoaded: true,
  percentPosition: false,
  dragThreshold: 20,
  selectedAttraction: 0.01,
  friction: 0.15,
});